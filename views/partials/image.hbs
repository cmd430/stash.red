<span class="wrapper">
  <a class="image" href="{{@direct}}/{{file_id}}.{{ext original_filename}}">
    <img src="{{@direct}}/{{file_id}}.{{ext original_filename}}" />
  </a>
  {{#if_eq @signedin.username this.uploaded_by this}}
    <div class="actions">
      <a class="action__delete" title="Delete this Image" data-id="/f/{{file_id}}" data-username="{{@signedin.username}}">
        <i class="icon-trash"></i>
      </a>
    </div>
  {{/if_eq}}
</span>
